apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "keda-poc.fullname" . }}-test-connection"
  labels:
    {{- include "keda-poc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "keda-poc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
